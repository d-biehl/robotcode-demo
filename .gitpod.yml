image:
  file: .gitpod.Dockerfile

tasks:
  - name: init dev environment
    init: >
      poetry install --no-root

vscode:
  extensions:
    - ms-python.python
    - esbenp.prettier-vscode

    - d-biehl.robotcode
