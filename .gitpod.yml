image:
  file: .gitpod.dockerfile

# tasks:
#   - name: init dev environment
#     before: poetry install --no-root --no-interaction
#     command: gp sync-done start

vscode:
  extensions:
    - ms-python.python
    - esbenp.prettier-vscode

    - d-biehl.robotcode
