*** Keywords ***
keyword with if statement

    IF  ${var}    asd


    IF    ${var} < 0
        Log to console some text
    ELSE
        Log to console some more text
    END
    more keywords
    yet more keywords
